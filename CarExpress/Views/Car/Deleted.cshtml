@model CarDeletedViewModel

@{
    ViewData["Title"] = "Supprimée";
}

<section>

    <div class="container text-center p-4 mt-5 m-4">
        <h1 class="display-6">@Model.Year @Model.Brand @Model.Model @Model.Trim</h1>
        <p>a bien été supprimée</p>
        <a asp-action="Index" asp-controller="Home" class="btn btn-primary mt-5 py-3">Retour à l'accueil</a>
    </div>

</section>